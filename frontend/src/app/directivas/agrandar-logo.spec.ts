import { AgrandarLogo } from './agrandar-logo';

describe('AgrandarLogo', () => {
  it('should create an instance', () => {
    const directive = new AgrandarLogo();
    expect(directive).toBeTruthy();
  });
});
